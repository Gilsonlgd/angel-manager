import './{{ pascal.layoutName }}Layout.scoped.css';
import { Outlet } from 'react-router-dom';

/*
 * import Navbar from "./components/Navbar/Navbar";
 * import Footer from "./components/Footer/Footer";
 */

function {{ pascal.layoutName }}Layout(): JSX.Element {
  return (
    <div {% raw %}style={{ height: '100%' }}{% endraw %}>
      {/* <NavBar /> */}
      <div className="page-content">
        <Outlet />
      </div>
      {/* <Footer /> */}
    </div>
  );
}

export default {{ pascal.layoutName }}Layout;

/**
 * @note
 * Note que no caso de uma arquitetura com diferentes níveis de usuário,
 * deve-se inserir o novo layout dentro de um diretório que leva o nome do nível.
 * Exemplo: PublicLayout == src/layout/public/PublicLayout.tsx
 */