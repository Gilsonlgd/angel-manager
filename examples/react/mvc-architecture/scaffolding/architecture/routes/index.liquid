/**
 * Exemplo de importação da view chamada "Home".
 * @note
 * Este é apenas um exemplo ilustrativo e deve ser substituido
 * pelas importações reais.
 */
import Home from '@views/{{ camel.routerName }}/Home/Home';

const {{ camel.routerName }}Routes = [
  /* Rota criada: {{ kebab.routerName }}/home */
  { path: 'home', element: <Home /> },
];

export default {{ camel.routerName }}Routes;

/**
 * @important
 * Não esqueça de importar {{ camel.routerName }}Routes em src/router.tsx
 * Após importar, inclua o objeto abaixo na listagem de rotas:
 * {
 *   path: '/{{ kebab.routerName }}',
 *   element: <{{ pascal.routerName }}Layout />,
 *   children: {{ camel.routerName }}Routes,
 * }
 */
