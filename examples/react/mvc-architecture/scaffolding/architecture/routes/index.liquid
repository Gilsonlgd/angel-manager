/**
 * Exemplo de importação de uma view chamada "Home".
 * @note
 * Este é apenas um exemplo ilustrativo e deve ser substituido
 * pelas importações reais.
 */
import Home from '@views/{{ camel.routerName }}/Home/Home';

const {{ camel.routerName }}Routes = [
  // A linha abaixo é apenas um exemplo. A substitua de acordo com as instruções durante a criação das views.
  { path: 'home', element: <Home /> },
];

export default {{ camel.routerName }}Routes;
